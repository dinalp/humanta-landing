---
phase: 01-foundation-and-layout
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Story.tsx
  - src/components/HowItWorks.tsx
  - src/components/Pricing.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - STORY-01
  - STORY-04
  - HIW-01
  - PRICE-01
  - PRICE-02

must_haves:
  truths:
    - "User sees 3 dark-background story panels with text (left) and image (right)"
    - "User sees key phrases highlighted in orange (#f5b614) accent text in story panels"
    - "User sees How Humanta Works section with 4 numbered steps on cream background"
    - "User sees 2 pricing cards with feature checklists, checkmarks, and Get in touch CTAs"
    - "Mobile: story panels stack vertically (image on top, text below)"
    - "Mobile: pricing cards stack vertically"
  artifacts:
    - path: "src/components/Story.tsx"
      provides: "3 story panels with text + image layout"
      contains: "brand-accent"
    - path: "src/components/HowItWorks.tsx"
      provides: "4-step section on cream background"
      contains: "how-it-works"
    - path: "src/components/Pricing.tsx"
      provides: "2 pricing cards with feature lists"
      contains: "PRICING_PLANS"
  key_links:
    - from: "src/components/Story.tsx"
      to: "src/lib/constants.ts"
      via: "import STORY_PANELS"
      pattern: "STORY_PANELS"
    - from: "src/components/HowItWorks.tsx"
      to: "src/lib/constants.ts"
      via: "import HOW_IT_WORKS_STEPS"
      pattern: "HOW_IT_WORKS_STEPS"
    - from: "src/components/Pricing.tsx"
      to: "src/lib/constants.ts"
      via: "import PRICING_PLANS"
      pattern: "PRICING_PLANS"
    - from: "src/app/page.tsx"
      to: "src/components/Story.tsx"
      via: "component import"
      pattern: "import.*Story"
---

<objective>
Build the three middle page sections — Story, How It Works, and Pricing — and wire them into the page below the Hero.

Purpose: These sections form the core value proposition (Story), process explanation (How It Works), and conversion mechanism (Pricing) of the landing page.
Output: Three new section components rendered on the page between Hero and the bottom sections (FAQ/CTA/Footer added in Plan 03).
</objective>

<execution_context>
@/Users/dinalperera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dinalperera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-layout/01-CONTEXT.md
@.planning/phases/01-foundation-and-layout/01-RESEARCH.md
@.planning/phases/01-foundation-and-layout/01-01-SUMMARY.md

<interfaces>
<!-- Types and constants created in Plan 01 that this plan consumes -->

From src/types/index.ts:
```typescript
export interface StoryPanel {
  title: string;
  description: string;
  highlightPhrase: string;
  image: string;
  imageAlt: string;
}

export interface Step {
  number: number;
  title: string;
  description: string;
}

export interface PricingPlan {
  name: string;
  subtitle: string;
  features: string[];
  ctaLabel: string;
  ctaHref: string;
}
```

From src/lib/constants.ts:
```typescript
export const STORY_PANELS: StoryPanel[]   // 3 panels with rewritten copy
export const HOW_IT_WORKS_STEPS: Step[]   // 4 numbered steps
export const PRICING_PLANS: PricingPlan[] // 2 plans (Essential + Pilot Package)
```

From src/app/globals.css (@theme inline):
```
--color-brand-dark: #0a0a0a
--color-brand-cream: #f5f3ee
--color-brand-accent: #f5b614
--color-brand-text: #070a06
--color-brand-text-secondary: #8a8a8a
```

Tailwind usage: `bg-brand-dark`, `bg-brand-cream`, `text-brand-accent`, `text-brand-text`, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Story, HowItWorks, and Pricing section components</name>
  <files>src/components/Story.tsx, src/components/HowItWorks.tsx, src/components/Pricing.tsx</files>
  <action>
**1. Create `src/components/Story.tsx`:**
Server component. Section `id="what-we-do"` (this is the "What we do" nav target).

- Dark background (`bg-brand-dark`) section with generous vertical padding (`py-24 lg:py-32`)
- Renders 3 panels from `STORY_PANELS` constant
- Each panel: flex layout, `md:flex-row` (text left, image right), `flex-col-reverse` on mobile (image on top, text below — per user decision)
- Alternate panels: even-indexed panels can optionally reverse to `md:flex-row-reverse` for visual variety
- Text side (~50% width on desktop): title in `font-heading` semibold `text-2xl md:text-3xl lg:text-4xl` white, description in `font-body text-white/70 text-base md:text-lg`, `highlightPhrase` rendered as an inline `<span>` with `text-brand-accent font-semibold` within the description text
- Image side (~50% width on desktop): next/image with explicit width/height (600x400), `rounded-2xl`, `sizes="(max-width: 768px) 100vw, 50vw"`, `object-cover`
- Gap between panels: `space-y-20 lg:space-y-32`
- Wrapped in `max-w-6xl mx-auto px-6`
- The highlighted phrase implementation: In constants.ts, the `description` field should contain a marker like `{{highlight}}phrase{{/highlight}}` or the component should find `highlightPhrase` within the description and wrap it in accent-colored span. Choose the simpler approach — if `highlightPhrase` is a substring of the description, use string split/replace to wrap it.

**2. Create `src/components/HowItWorks.tsx`:**
Server component. Section `id="how-it-works"`.

- Cream background (`bg-brand-cream`) section, `py-24 lg:py-32`
- Heading: "How Humanta Works" in `font-heading font-semibold text-3xl md:text-4xl text-brand-text text-center mb-16`
- 4 steps from `HOW_IT_WORKS_STEPS` in a responsive grid: `grid md:grid-cols-2 lg:grid-cols-4 gap-8`
- Each step card: step number in a `w-14 h-14` circle with `bg-brand-accent text-white rounded-full` centered, title in `font-heading font-semibold text-brand-text`, description in `text-brand-text-secondary text-sm`
- Wrapped in `max-w-6xl mx-auto px-6`
- Note: The tab/stepper interaction (HIW-02) is Phase 3. This is static layout only — all 4 steps visible simultaneously.

**3. Create `src/components/Pricing.tsx`:**
Server component. Section `id="plans"` (this is the "Plans" nav target).

- Dark or cream background (use cream `bg-brand-cream` to match the warm premium feel, OR use dark with white cards — check humanta.co for reference). Per Claude's discretion: use cream background with white cards for warmth.
- Heading: centered, `font-heading font-semibold text-3xl md:text-4xl text-brand-text`
- 2 cards from `PRICING_PLANS` in `grid md:grid-cols-2 gap-8 max-w-4xl mx-auto`
- Each card: `bg-white rounded-2xl p-8 shadow-sm border border-gray-100`
  - Plan name: `font-heading text-xl font-semibold text-brand-text`
  - Subtitle: `text-brand-text-secondary`
  - Feature list: `<ul>` with checkmark SVG icon (accent colored) + feature text for each item
  - CTA button: `bg-brand-dark text-white rounded-full py-3 w-full text-center font-medium hover:bg-brand-dark/90 transition-colors`
- Checkmark SVG: inline `<svg>` with `viewBox="0 0 20 20"`, `fill="currentColor"`, `className="w-5 h-5 text-brand-accent"`
- Section padding: `py-24 lg:py-32`, wrapped in `max-w-6xl mx-auto px-6`
- Mobile: cards stack vertically (grid handles this automatically with md: prefix)
  </action>
  <verify>
    <automated>cd /Users/dinalperera/projects/humanta-landing && npm run build</automated>
  </verify>
  <done>Story.tsx renders 3 panels with text+image+accent highlights. HowItWorks.tsx renders 4 steps on cream background. Pricing.tsx renders 2 cards with feature checklists and checkmarks. All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Story, HowItWorks, and Pricing into page</name>
  <files>src/app/page.tsx</files>
  <action>
Update `src/app/page.tsx` to import and render the three new section components below the Hero:

```tsx
import { Navbar } from "@/components/Navbar";
import { Hero } from "@/components/Hero";
import { Story } from "@/components/Story";
import { HowItWorks } from "@/components/HowItWorks";
import { Pricing } from "@/components/Pricing";

export default function Home() {
  return (
    <>
      <Navbar />
      <main>
        <Hero />
        <Story />
        <HowItWorks />
        <Pricing />
        {/* FAQ, CTA Banner, and Footer added in Plan 03 */}
      </main>
    </>
  );
}
```

Section order: Hero -> Story -> How It Works -> Pricing. Per user decision and Claude's discretion, story can be repositioned lower if B2B best practices warrant it. However, the standard B2B landing page flow (problem/story -> process -> pricing) makes this order logical. Keep it.

Verify that nav anchor links (#what-we-do, #how-it-works, #plans) correctly correspond to the section IDs.
  </action>
  <verify>
    <automated>cd /Users/dinalperera/projects/humanta-landing && npm run build && npm run lint</automated>
  </verify>
  <done>Page renders Hero -> Story -> HowItWorks -> Pricing in order. Nav links scroll to correct sections. Build and lint pass clean.</done>
</task>

</tasks>

<verification>
- `npm run build && npm run lint` passes
- Dev server shows all sections in order: Hero -> Story (3 dark panels) -> How It Works (cream, 4 steps) -> Pricing (2 cards)
- Story panels show orange accent text on key phrases
- How It Works has cream background with 4 numbered step circles
- Pricing shows 2 cards with checkmark feature lists and "Get in touch" buttons
- Nav links #what-we-do, #how-it-works, #plans scroll to correct sections
</verification>

<success_criteria>
- 3 story panels render with text left / image right (desktop), image top / text below (mobile)
- Orange #f5b614 accent highlights visible on key phrases in story
- 4 How It Works steps on cream #f5f3ee background
- 2 pricing cards with feature checklists, checkmark icons, and CTA buttons
- All sections use content from constants.ts (no hardcoded text in components)
- Responsive: mobile stacks panels and cards vertically
- Build passes, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-layout/01-02-SUMMARY.md`
</output>
