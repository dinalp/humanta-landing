---
phase: 01-foundation-and-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/types/index.ts
  - src/lib/constants.ts
  - src/components/Navbar.tsx
  - src/components/Hero.tsx
  - public/images/hero/hero-bg.jpg
  - public/images/story/panel-1.jpg
  - public/images/story/panel-2.jpg
  - public/images/story/panel-3.jpg
  - public/images/cta/cta-bg.jpg
autonomous: true
requirements:
  - NAV-01
  - NAV-03
  - NAV-05
  - HERO-01
  - GLOBAL-09

must_haves:
  truths:
    - "User sees a sticky navbar with Humanta logo, section links (What we do, How it works, Plans), and Get in touch button"
    - "Nav is transparent over the hero and transitions to solid dark background on scroll"
    - "User can click nav links and the page smooth-scrolls to anchor targets"
    - "User sees a full-viewport hero with background image, tagline, headline, description, and Start now CTA"
    - "All images are self-hosted in /public (downloaded from humanta.co)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand color tokens, font theme, base styles"
      contains: "@theme inline"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter, Manrope, Poppins fonts"
      contains: "font-inter"
    - path: "src/lib/constants.ts"
      provides: "All content data for every section"
      exports: ["NAV_LINKS", "STORY_PANELS", "HOW_IT_WORKS_STEPS", "PRICING_PLANS", "FAQ_ITEMS"]
    - path: "src/types/index.ts"
      provides: "Shared TypeScript interfaces"
      exports: ["StoryPanel", "Step", "PricingPlan", "FAQItem"]
    - path: "src/components/Navbar.tsx"
      provides: "Sticky nav with scroll-based background transition"
      contains: "use client"
    - path: "src/components/Hero.tsx"
      provides: "Full-viewport hero section"
      contains: "next/image"
  key_links:
    - from: "src/components/Navbar.tsx"
      to: "src/lib/constants.ts"
      via: "import NAV_LINKS"
      pattern: "NAV_LINKS"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import globals.css"
      pattern: "import.*globals"
    - from: "src/app/page.tsx"
      to: "src/components/Navbar.tsx"
      via: "component import"
      pattern: "import.*Navbar"
---

<objective>
Set up the complete project foundation (brand theme, fonts, color tokens, content data, TypeScript types, image assets) and build the Navbar and Hero — the first two things a user sees.

Purpose: Establishes all shared patterns (fonts, colors, component architecture, content data structure) that every subsequent plan depends on. Navbar and Hero are the above-the-fold experience.
Output: Working Navbar + Hero on localhost, all content constants and types ready for remaining sections.
</objective>

<execution_context>
@/Users/dinalperera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dinalperera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-layout/01-CONTEXT.md
@.planning/phases/01-foundation-and-layout/01-RESEARCH.md

<interfaces>
<!-- Existing files that will be replaced/modified -->

From src/app/layout.tsx (current — will be rewritten):
- Only has Poppins font, needs Inter and Manrope added
- Metadata is good, keep it

From src/app/globals.css (current — will be rewritten):
- Has WRONG brand colors (orange, mint, yellow, teal gradients)
- Must be replaced with: dark #0a0a0a, cream #f5f3ee, accent #f5b614, text #070a06

From public/:
- logo-black.png and logo-white.png already exist
- Need to create images/hero/, images/story/, images/cta/ directories
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foundation — theme, fonts, types, constants, and image assets</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/types/index.ts, src/lib/constants.ts, public/images/hero/, public/images/story/, public/images/cta/</files>
  <action>
**1. Download images from humanta.co:**
Visit humanta.co in the browser context or use curl to download all site images. Save to:
- `/public/images/hero/hero-bg.jpg` — the hero background (full-bleed photo of people dining/connecting)
- `/public/images/story/panel-1.jpg`, `panel-2.jpg`, `panel-3.jpg` — the three story section images
- `/public/images/cta/cta-bg.jpg` — the CTA banner background image

If images cannot be downloaded programmatically (Framer JS-rendered), create placeholder images using a solid color with descriptive text, and add a comment noting they need manual replacement. The key is that the file paths exist and components reference them correctly.

**2. Rewrite `src/app/globals.css`:**
Strip ALL existing content. Replace with:
```css
@import "tailwindcss";

@theme inline {
  --font-sans: var(--font-inter);
  --font-heading: var(--font-manrope);
  --font-body: var(--font-poppins);

  --color-brand-dark: #0a0a0a;
  --color-brand-cream: #f5f3ee;
  --color-brand-accent: #f5b614;
  --color-brand-text: #070a06;
  --color-brand-text-secondary: #8a8a8a;
  --color-brand-border: rgba(255, 255, 255, 0.1);
}

html {
  scroll-behavior: smooth;
}

body {
  background: var(--color-brand-dark);
  color: white;
}

::selection {
  background: var(--color-brand-accent);
  color: black;
}
```

CRITICAL: Use `@theme inline` (not `@theme`) for font variables — they are set via className on the html element at runtime, not available at CSS build time.

**3. Rewrite `src/app/layout.tsx`:**
- Import Inter, Manrope, Poppins from `next/font/google` with `display: "swap"` and CSS variable names
- Keep existing metadata (title, description, keywords, openGraph)
- Apply all three font variables to `<html>` className
- Body className: `font-body antialiased`

**4. Create `src/types/index.ts`:**
```typescript
export interface StoryPanel {
  title: string;
  description: string;
  highlightPhrase: string;
  image: string;
  imageAlt: string;
}

export interface Step {
  number: number;
  title: string;
  description: string;
}

export interface PricingPlan {
  name: string;
  subtitle: string;
  features: string[];
  ctaLabel: string;
  ctaHref: string;
}

export interface FAQItem {
  question: string;
  answer: string;
}
```

**5. Create `src/lib/constants.ts`:**
Populate with ALL content data for the entire site. This is the single source of truth for text content.

- `NAV_LINKS`: [{label: "What we do", href: "#what-we-do"}, {label: "How it works", href: "#how-it-works"}, {label: "Plans", href: "#plans"}]
- `HERO_CONTENT`: {tagline: "Make human connection company policy", headline: "Humanta", description: [pull exact copy from humanta.co — the paragraph about curated dinner experiences], ctaLabel: "Start now", ctaHref: "#plans"}
- `STORY_PANELS`: 3 panels with REWRITTEN copy (per user decision). Current humanta.co story is too wordy and doesn't impact HR/People leaders. Rewrite to be:
  - Shorter (2-3 sentences per panel max)
  - Focused on HR pain points: retention, engagement, culture
  - Each panel has a `highlightPhrase` (the orange-accented key phrase)
  - Keep the visual structure (text + image), change the words
  - Panel themes: (1) The problem — disconnected teams, (2) The insight — real connection happens over food, (3) The solution — Humanta makes it effortless
- `HOW_IT_WORKS_STEPS`: 4 steps [pull exact copy from humanta.co]
- `PRICING_PLANS`: 2 plans — Essential (per redeemed spark) and Pilot Package Sydney (10 Humanta Sparks) with feature checklists [pull exact copy from humanta.co]
- `FAQ_ITEMS`: 6 questions and answers [pull exact copy from humanta.co]
- `CTA_CONTENT`: {headline: "Ready to keep and attract the best talent?", ctaLabel: "Let's Talk", ctaHref: "#contact"}
- `FOOTER_LINKS`, `SOCIAL_LINKS`: section links + LinkedIn/Instagram/TikTok URLs

For content that cannot be pulled from humanta.co (Framer JS-rendered), write realistic placeholder content that matches the brand voice (warm, human, premium B2B) and add a `// TODO: verify against humanta.co` comment. The user can verify and adjust.

All arrays and objects should be typed using the interfaces from `src/types/index.ts`.
  </action>
  <verify>
    <automated>cd /Users/dinalperera/projects/humanta-landing && npm run build</automated>
  </verify>
  <done>globals.css has brand colors (no gradients), layout.tsx has 3 fonts, types/index.ts exports all interfaces, constants.ts has all section content data, image files exist in public/images/</done>
</task>

<task type="auto">
  <name>Task 2: Navbar and Hero components wired into page</name>
  <files>src/components/Navbar.tsx, src/components/Hero.tsx, src/app/page.tsx</files>
  <action>
**1. Create `src/components/Navbar.tsx`:**
Client component (`"use client"`) with:
- Fixed positioning, z-50, full-width
- Scroll detection via `useEffect` + `useState`: transparent when scrollY < 50, solid dark with backdrop-blur when scrolled
- Layout: logo (left, use `/logo-white.png` with next/image, ~120px wide), nav links (center, horizontal on desktop, hidden on mobile — mobile menu is Phase 3), "Get in touch" CTA button (right, accent color rounded-full)
- Nav links from `NAV_LINKS` constant, rendered as `<a>` tags with `href` pointing to section anchors
- Smooth scroll works via CSS `scroll-behavior: smooth` on html (already set in globals.css) — no JS scroll needed
- Transition: `transition-all duration-300` on the nav container for smooth background change
- Desktop: horizontal layout with flexbox, items-center, justify-between
- Mobile: logo + CTA visible, nav links hidden (will be hamburger menu in Phase 3)
- Padding: `py-4 px-6 lg:px-12`

**2. Create `src/components/Hero.tsx`:**
Server component (no "use client") with:
- Full viewport height: `min-h-screen` with `relative` positioning
- Background image using next/image with `fill`, `priority`, `sizes="100vw"`, `className="object-cover"`
- Dark overlay: `absolute inset-0 bg-black/40 z-[1]` for text readability
- Content centered vertically and horizontally: `relative z-10 flex flex-col items-center justify-center text-center`
- Tagline: smaller text, uppercase or tracking-wide, `font-body` (Poppins)
- Headline: "Humanta" in `font-heading` (Manrope), very large (`text-5xl md:text-7xl lg:text-8xl`), `font-bold`
- Description paragraph: max-w-2xl, `text-lg md:text-xl`, `font-body`, text-white/80
- "Start now" CTA: rounded-full button, accent background, dark text, `font-medium`, links to `#plans`
- Content imported from `HERO_CONTENT` constant
- Section has `id="hero"`

**3. Rewrite `src/app/page.tsx`:**
Strip ALL existing placeholder content. Replace with clean composition:
```tsx
import { Navbar } from "@/components/Navbar";
import { Hero } from "@/components/Hero";

export default function Home() {
  return (
    <>
      <Navbar />
      <main>
        <Hero />
        {/* Remaining sections added in Plans 02 and 03 */}
      </main>
    </>
  );
}
```

Keep page.tsx as a server component (no "use client"). Navbar is the only client component.
  </action>
  <verify>
    <automated>cd /Users/dinalperera/projects/humanta-landing && npm run build</automated>
  </verify>
  <done>Navbar renders with logo, 3 section links (desktop), and "Get in touch" CTA. Nav transitions from transparent to solid on scroll. Hero shows full-viewport background image with tagline, headline, description, and "Start now" button. Page builds without errors.</done>
</task>

</tasks>

<verification>
- `npm run build` passes (TypeScript compiles, no build errors)
- `npm run lint` passes (ESLint clean)
- Dev server (`npm run dev`) shows: sticky navbar with logo and links, full-screen hero with background image and content
- Scrolling down changes navbar from transparent to solid dark background
- All brand colors are dark/cream/accent (no gradient colors from old theme)
</verification>

<success_criteria>
- Brand theme fully replaced: dark #0a0a0a, cream #f5f3ee, accent #f5b614 (zero old gradient colors)
- Three fonts configured and available as Tailwind utilities (font-sans, font-heading, font-body)
- All content data centralized in constants.ts with TypeScript types
- Images downloaded/created in public/images/ directory structure
- Navbar is sticky, transparent-to-solid on scroll, has logo + links + CTA
- Hero is full-viewport with background image, headline, description, CTA
- Nav links have correct href anchors (#what-we-do, #how-it-works, #plans)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-layout/01-01-SUMMARY.md`
</output>
