---
phase: 01-foundation-and-layout
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/components/FAQ.tsx
  - src/components/CTABanner.tsx
  - src/components/Footer.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - FAQ-01
  - CTA-01
  - FOOT-01
  - GLOBAL-06

must_haves:
  truths:
    - "User sees Everything you need to know section with 6 FAQ questions displayed"
    - "User sees full-width CTA banner with warm background image, headline, and Let's Talk button"
    - "User sees footer with Humanta logo, tagline, section links, social links (LinkedIn, Instagram, TikTok), and privacy policy link"
    - "User can view the complete page on mobile (375px), tablet (768px), and desktop (1280px) with appropriate responsive layouts"
    - "All page sections are present: Nav, Hero, Story, How It Works, Pricing, FAQ, CTA Banner, Footer"
  artifacts:
    - path: "src/components/FAQ.tsx"
      provides: "6 FAQ questions in static layout"
      contains: "FAQ_ITEMS"
    - path: "src/components/CTABanner.tsx"
      provides: "Full-width CTA with background image"
      contains: "CTA_CONTENT"
    - path: "src/components/Footer.tsx"
      provides: "Footer with logo, links, socials"
      contains: "FOOTER_LINKS"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/FAQ.tsx"
      via: "component import"
      pattern: "import.*FAQ"
    - from: "src/app/page.tsx"
      to: "src/components/Footer.tsx"
      via: "component import"
      pattern: "import.*Footer"
    - from: "src/components/CTABanner.tsx"
      to: "public/images/cta/cta-bg.jpg"
      via: "next/image src"
      pattern: "cta-bg"
---

<objective>
Build the final three sections (FAQ, CTA Banner, Footer), complete the page assembly, and verify the full responsive layout across breakpoints.

Purpose: Completes the full landing page with all sections. The visual checkpoint confirms the entire page matches the premium brand aesthetic before moving to animations (Phase 2) and interactions (Phase 3).
Output: Complete static landing page with all 8 sections, verified responsive across mobile/tablet/desktop.
</objective>

<execution_context>
@/Users/dinalperera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dinalperera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-layout/01-CONTEXT.md
@.planning/phases/01-foundation-and-layout/01-RESEARCH.md
@.planning/phases/01-foundation-and-layout/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-layout/01-02-SUMMARY.md

<interfaces>
<!-- Types and constants from Plan 01 -->

From src/types/index.ts:
```typescript
export interface FAQItem {
  question: string;
  answer: string;
}
```

From src/lib/constants.ts:
```typescript
export const FAQ_ITEMS: FAQItem[]     // 6 FAQ questions and answers
export const CTA_CONTENT: { headline: string; ctaLabel: string; ctaHref: string }
export const FOOTER_LINKS: { label: string; href: string }[]
export const SOCIAL_LINKS: { platform: string; href: string; icon: string }[]
```

From globals.css theme tokens:
```
bg-brand-dark, bg-brand-cream, text-brand-accent, text-brand-text, text-brand-text-secondary
font-heading (Manrope), font-body (Poppins), font-sans (Inter)
```

From page.tsx (current state after Plan 02):
```tsx
// Has: Navbar, Hero, Story, HowItWorks, Pricing
// Needs: FAQ, CTABanner, Footer added below Pricing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAQ, CTA Banner, and Footer components + page assembly</name>
  <files>src/components/FAQ.tsx, src/components/CTABanner.tsx, src/components/Footer.tsx, src/app/page.tsx</files>
  <action>
**1. Create `src/components/FAQ.tsx`:**
Server component. Section with dark background (`bg-brand-dark`), `py-24 lg:py-32`.

- Heading: "Everything you need to know" in `font-heading font-semibold text-3xl md:text-4xl text-white text-center mb-16`
- Render 6 FAQ items from `FAQ_ITEMS` constant
- Static layout only (accordion expand/collapse is Phase 3 FAQ-02)
- Each FAQ item: a container with bottom border (`border-b border-brand-border`), question as `font-heading font-medium text-lg text-white py-5`, answer visible below in `text-white/70 font-body text-base pb-5`
- Note: In Phase 3, these will become collapsible — structure the HTML so it's easy to add toggle state (question in a button/div, answer in a div that can be height-animated). For now, all answers are visible.
- Max width container: `max-w-3xl mx-auto px-6`

**2. Create `src/components/CTABanner.tsx`:**
Server component. Full-width section.

- Background image using next/image `fill` + `object-cover`, `sizes="100vw"` (from `/images/cta/cta-bg.jpg`)
- Dark overlay for text readability: `absolute inset-0 bg-black/50`
- Content centered: `relative z-10 text-center py-24 lg:py-32 px-6`
- Headline from `CTA_CONTENT.headline`: `font-heading font-bold text-3xl md:text-4xl lg:text-5xl text-white max-w-3xl mx-auto mb-8`
- CTA button: `bg-brand-accent text-brand-dark font-semibold px-8 py-4 rounded-full text-lg hover:bg-brand-accent/90 transition-colors` linked to `CTA_CONTENT.ctaHref`

**3. Create `src/components/Footer.tsx`:**
Server component. Dark background, `py-16 border-t border-brand-border`.

- Layout: `max-w-6xl mx-auto px-6`
- Top section (flex, responsive):
  - Left: Humanta logo (`/logo-white.png`, next/image ~100px wide) + tagline text in `text-white/60 text-sm mt-2`
  - Center/Right: Section links (from `FOOTER_LINKS`) in a horizontal list on desktop, vertical on mobile
  - Social links: LinkedIn, Instagram, TikTok — use simple SVG icons or text links. Each opens in new tab (`target="_blank" rel="noopener noreferrer"`)
- Bottom section: `border-t border-brand-border mt-8 pt-8`, flex between copyright text ("2024 Humanta. All rights reserved.") and privacy policy link
- Responsive: On mobile, stack vertically with `flex-col` gap; on desktop, `md:flex-row` with justify-between

**4. Update `src/app/page.tsx`:**
Add the three new components to complete the full page:

```tsx
import { Navbar } from "@/components/Navbar";
import { Hero } from "@/components/Hero";
import { Story } from "@/components/Story";
import { HowItWorks } from "@/components/HowItWorks";
import { Pricing } from "@/components/Pricing";
import { FAQ } from "@/components/FAQ";
import { CTABanner } from "@/components/CTABanner";
import { Footer } from "@/components/Footer";

export default function Home() {
  return (
    <>
      <Navbar />
      <main>
        <Hero />
        <Story />
        <HowItWorks />
        <Pricing />
        <FAQ />
        <CTABanner />
      </main>
      <Footer />
    </>
  );
}
```

Footer is outside `<main>` (semantic HTML).
  </action>
  <verify>
    <automated>cd /Users/dinalperera/projects/humanta-landing && npm run build && npm run lint</automated>
  </verify>
  <done>Full page renders all 8 sections in order: Navbar, Hero, Story, How It Works, Pricing, FAQ, CTA Banner, Footer. Build and lint pass. All content sourced from constants.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete landing page</name>
  <files>n/a</files>
  <action>
Human verifies the complete static landing page across desktop and mobile breakpoints.

What was built: Complete static landing page with all sections — Navbar (sticky, transparent-to-solid), Hero (full-viewport), Story (3 panels), How It Works (4 steps), Pricing (2 cards), FAQ (6 questions), CTA Banner (background image), and Footer (logo, links, socials). Responsive across breakpoints. Brand colors: dark + cream + orange accent. Three fonts: Inter, Manrope, Poppins.

How to verify:
1. Run `npm run dev` and visit http://localhost:3000
2. Desktop check (1280px+):
   - Navbar: logo left, 3 links center, "Get in touch" button right
   - Scroll down — navbar transitions from transparent to solid dark
   - Click nav links — page smooth-scrolls to correct sections
   - Hero: full-screen with background image, tagline, "Humanta" headline, description, "Start now" button
   - Story: 3 panels, text left / image right, orange accent highlights
   - How It Works: cream background, 4 numbered steps
   - Pricing: 2 cards with checkmark feature lists and CTA buttons
   - FAQ: 6 questions with answers visible
   - CTA Banner: warm background image with headline and "Let's Talk" button
   - Footer: logo, links, social icons, copyright
3. Mobile check (375px via DevTools):
   - Nav shows logo and CTA only (links hidden, hamburger menu is Phase 3)
   - Hero content stacks and is readable
   - Story panels: image on top, text below
   - Pricing cards: stacked vertically
   - All sections fit within viewport width (no horizontal scroll)
4. Overall: Does the site feel premium, warm, and human? Compare against humanta.co for visual parity.

Resume signal: Type "approved" to complete Phase 1, or describe specific issues to fix.
  </action>
  <verify>Human approval</verify>
  <done>User confirms the landing page looks premium, warm, and human across desktop and mobile. All sections present with correct content, images, typography, and responsive behavior.</done>
</task>

</tasks>

<verification>
- `npm run build && npm run lint` passes clean
- All 8 sections render on the page in correct order
- Nav anchor links (#what-we-do, #how-it-works, #plans) scroll to correct sections
- No horizontal overflow on any breakpoint
- No old gradient/multi-color scheme visible anywhere
- All images load (or placeholders visible with TODO comments)
- Footer social links open in new tabs
</verification>

<success_criteria>
- Complete landing page with all sections visible
- FAQ shows 6 questions with answers (static, no accordion yet)
- CTA Banner has background image with overlay and centered headline + button
- Footer has logo, tagline, section links, social links, privacy link, copyright
- Responsive: mobile (375px), tablet (768px), desktop (1280px) all look correct
- Human verification confirms premium brand aesthetic
- Zero old placeholder content or gradient colors remaining
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-layout/01-03-SUMMARY.md`
</output>
